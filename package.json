{
  "name": "com.swaglabs.playwright",
  "version": "1.0.0",
  "description": "```bash\r npm install\r npx playwright install\r ```",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --headed --project chromium",
    "test:container": "docker build -t swaglabs . && docker run swaglabs",
    "test:echo": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.46.1",
    "playwright-chromium": "^1.46.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.1",
    "@types/node": "^22.5.0"
  }
}
